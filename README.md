# ha

5 week initial set up plan:

1. Learning with SystemVerilog Syntax and RTL Design. With CUDA Python integration and understanding GPU parallelism for embedded DSP Design. Looking to memory heirarchal structures.
2. Hardware setup with ESP32 and MEMS systems. Have to buy them ASAP and understand everything about it, so I don't waste time while trying to integrate the hardware components. Collecting Sample data in order to start a dataset and use TensorFlow(?). Aim to have a deliverable setup with tests.
3. ESP 32 running with an Arduino IDE (hopefully) and able to handle buffer. Should do elementary tasks for now.
4. Generate the UVM testbench and have the DSP block simulated, ready for generating a response framework. Hopefully the simple SystemVerilog DSP block can handle the tasks and work as efficiently as possible.
5. Connect to Firebase or SQL and run final tests. Have it connected to the cloud so it can remember which tasks are the recognized set.
6. Rollout.

Voice-Activated IoT Assistant with Edge AI & DSP Acceleration.

Concepts of SystemVerilog, CUDA and hardware components in ESP32.
